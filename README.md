# magoni
My dotfiles.

## TODO
```
# include the following submodules
git clone https://github.com/chriskempson/base16-shell.git ~/.config/base16-shell
git clone git://github.com/airblade/vim-gitgutter.git
# https://vimawesome.com/plugin/ale
# https://vimawesome.com/plugin/closetag-vim
# vim-fugitive

# symlinks script

# get latest vim
brew install vim
sudo mv /usr/bin/vi /usr/bin/vi-old
vim --version | grep python # ensure new version includes python3

# powerline
brew install python
brew upgrade python
pip3 install --upgrade pip setuptools wheel
pip install --user powerline-status # should use pip3 here?
```
